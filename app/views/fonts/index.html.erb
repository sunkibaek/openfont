<div class="col-lg-10">
  <input ng-model="example.text" type="text" class="form-control" placeholder="오픈폰트는 웹에서 자유롭게 사용할 수 있는 우리말 폰트를 제공합니다" />
</div>

<div class="col-lg-2">
  <select ng-model="example.size" ng-init="example.size='14px'" class="form-control">
    <option value="8px">8px</option>
    <option value="9px">9px</option>
    <option value="10px">10px</option>
    <option value="11px">11px</option>
    <option value="12px">12px</option>
    <option value="14px">14px</option>
    <option value="16px">16px</option>
    <option value="18px">18px</option>
    <option value="20px">20px</option>
    <option value="22px">22px</option>
    <option value="24px">24px</option>
    <option value="26px">26px</option>
    <option value="28px">28px</option>
    <option value="36px">36px</option>
    <option value="48px">48px</option>
    <option value="72px">72px</option>
  </select>
</div>

<table class="table top_margin">
  <thead>
    <tr>
      <th>폰트</th>
      <th colspan="5">지원형식</th>
      <th>종류</th>
      <th>스타일</th>
      <th>두께</th>
    </tr>
  </thead>

  <tbody>
    <% @fonts.each do |font| %>
      <tr>
        <td><%= link_to font.name, font %></td>
        <td><span class="label label-<%= font.eot ? 'success' : 'default'  %>">eot</span></td>
        <td><span class="label label-<%= font.woff ? 'success' : 'default'  %>">woff</span></td>
        <td><span class="label label-<%= font.woff2 ? 'success' : 'default'  %>">woff2</span></td>
        <td><span class="label label-<%= font.ttf ? 'success' : 'default'  %>">ttf</span></td>
        <td><span class="label label-<%= font.svg ? 'success' : 'default'  %>">svg</span></td>
        <td><%= font.font_type.name %></td>
        <td><%= font.style %></td>
        <td><%= font.weight %></td>
      </tr>
      <tr>
        <td colspan="9" style="border-top:none;"><span style="font-family: '<%= font.family_name %>', <%= font.font_type.name %>;font-style:<%= font.style %>;font-weight:<%= font.weight %>;font-size:{{example.size}};">{{example.text || "오픈폰트는 웹에서 자유롭게 사용할 수 있는 우리말 폰트를 제공합니다"}}</span></td>
      </tr>
    <% end %>
  </tbody>
</table>

<style type="text/css">
  <% @fonts.each do |font| %>
    <%= render 'css_code', font: font %>
  <% end %>
</style>